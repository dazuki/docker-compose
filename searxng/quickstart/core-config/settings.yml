use_default_settings:
  engines:
    keep_only:
      - google
      - duckduckgo
      - brave
      - bing

server:
  secret_key: "V3ryV3ryS3cr3tK3y"  # generate: openssl rand -hex 32
  limiter: false            # internal service, not publicly exposed

search:
  formats:
    - html
    - json  # required for MCP / AI assistant

ui:
  default_lang: "en"

valkey:
  url: valkey://searxng-valkey:6379/0
