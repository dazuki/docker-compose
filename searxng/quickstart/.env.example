# Copy to .env and fill in values

# SearXNG
SEARXNG_VERSION=latest

# MCP server
MCP_HOST=127.0.0.1  # IP NPM forwards to
MCP_PORT=3000       # port NPM forwards to
MCP_AUTH_TOKEN=     # generate: openssl rand -hex 32
